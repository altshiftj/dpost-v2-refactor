import os
import re
import logging
from kadi_apy import KadiManager

DEVICE_ID = "REM_01"

WATCH_DIR = os.path.abspath("monitored_folder")
RENAME_DIR = os.path.join(WATCH_DIR, 'To_Rename')
RECORD_DIR = os.path.join(WATCH_DIR, 'Records')
EXCEPTIONS_DIR = os.path.join(WATCH_DIR, 'Exceptions')
ARCHIVED_FILES_JSON = os.path.join(RECORD_DIR, 'archive_db.json')
DAILY_RECORDS_JSON = os.path.join(RECORD_DIR, 'daily_records.json')

FILENAME_PATTERN = re.compile(r'^[A-Za-z0-9]+_[A-Za-z0-9]+_[A-Za-z0-9-]+$')

TESTING = False
TESTING_PATH = ""

LOG_FILE = 'watchdog.log'

SESSION_TIMEOUT = 300 # 5 minutes

DEBOUNCE_TIME = 1.0

DEFAULT_REM_RECORD_DESCRIPTION = """
# Default Description

**Please take time to describe the data contained within this record particularly if it is valuable and especially if it is used in publication**
/n
*This entry contains data generated by a PhenomXL TischREM manufactured by Thermofischer*

###### Data from the PhenomXL TischREM
The device is capable of producing a multitude of data, the majority of which being tiff images, image files with extra metadata attached describing the details of the measurement settings.

The device is also capable of performing element identification, either at a point, along a line, or within a region of an image. This results in an folder containing basic documentation, images, and graphs describing the element measurement process and results.

Finally the device is capable of surface profiling, though it is seldom used. This returns a folder containing documentation, images,and heat maps describing the surface profile of a sample.
"""
