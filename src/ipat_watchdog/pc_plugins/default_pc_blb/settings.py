from pathlib import Path
import re
from typing import List, Pattern, Optional, Tuple
import os
from ipat_watchdog.core.config.global_settings import PCSettings

class DefaultPCSettings(PCSettings):
    """Default PC settings that use the base PCSettings configuration."""
    pass
