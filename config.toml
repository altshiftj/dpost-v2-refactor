# Default mode (safer for general use)
model = "gpt-5.3-codex"
model_reasoning_effort = "high"
personality = "pragmatic"
approval_policy = "on-request"
sandbox_mode = "workspace-write"

[projects.'D:\Repos\ipat_data_watchdog']
trust_level = "trusted"

[windows]
sandbox = "unelevated"

# Phase 9 autonomous mode (explicit opt-in)
[profiles.phase9_autonomous]
model = "gpt-5.3-codex"
model_reasoning_effort = "xhigh"
personality = "pragmatic"
approval_policy = "never"
sandbox_mode = "workspace-write"