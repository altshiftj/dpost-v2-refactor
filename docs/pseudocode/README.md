# Folder Documentation: docs/pseudocode

## Archive Status

This folder is retained as an **archived V1->V2 migration traceability corpus**.
It documents historical pseudocode and source-mapping context used during the
rewrite period.

## Active Source of Truth

For current runtime behavior and ownership, use:

- `src/dpost_v2/`
- `tests/dpost_v2/`
- `docs/architecture/`
- active checklists/reports in `docs/checklists/` and `docs/reports/`

## Notes

- Legacy path references (for example `src/dpost/**`) in this folder are
  historical and do not define active runtime targets.
- Retired architecture modes (`v1`, `shadow`) may appear in historical specs;
  they are not active runtime modes.
