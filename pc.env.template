# PC Plugin Environment Template
# Copy this to pc.env and modify as needed

# PC_NAME determines which PC plugin to load
# Available options: default_pc_blb, lab_workstation_blb
PC_NAME=default_pc_blb

# Device plugins to load (can be multiple, comma-separated)
DEVICE_NAMES=sem_phenomxl2
